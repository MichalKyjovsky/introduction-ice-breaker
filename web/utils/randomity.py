import random


def shuffle_students(students: list) -> list:
    random.shuffle(students)
    return students

def shuffle_topics(topics: list) -> list:
    random.shuffle(topics)
    return topics
